{
  "name": "@yotlich/hw-ts-starter",
  "version": "0.1.0",
  "description": "",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "format": "npx prettier --write",
    "format:all": "npm run format .",
    "prepare": "npx simple-git-hooks"
  },
  "devDependencies": {
    "lint-staged": "^16.3.3",
    "prettier": "^3.8.1",
    "simple-git-hooks": "^2.13.1"
  },
  "simple-git-hooks": {
    "pre-commit": "npx lint-staged"
  },
  "lint-staged": {
    "*": [
      "npm run format"
    ]
  }
}
